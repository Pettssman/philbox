# philbox

## Gitophil
Gitophil is used as a git tool to create branches, commits and PRs.

Run the following to bind the executable to the alias "gitophil", the alias can be set to whatever you want:\
`git config --local alias.gitophil 'PATH_TO_FILE.exe'`

Then run:\
`git gitophil`
